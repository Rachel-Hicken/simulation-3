const initial_state = {
    username: '',
    id: '',
    img: ''
}




export default function reducer(state = initial_state, action) {
    switch (action.type) {
        default:
            return state;
    }
}